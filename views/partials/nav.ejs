<nav class="navbar navbar-default navbar-static-top navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Russian Soccer stat</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= currentYear %><span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <% for (var i = 0; i < years.length; i++) { %>
                        <li><a href="<%= url %>/<%= years[i] %>"><%= years[i] %></a></li>
                        <% } %>
                    </ul>
                </li>
                <% for (var i = 0; i < nav.length; i++) { %>
                <% if (nav[i].active) { %>
                    <li class="active">
                <% } else { %>
                    <li>
                <% }  %>
                    <% var nav_url = nav[i].url + '/' + currentYear %>
                    <% if (nav[i].url == '/') { nav_url = '/' } %>
                        <a href="<%= nav_url %>"><span class="glyphicon glyphicon-<%= nav[i].icon %>"></span> <%= nav[i].title %></a>
                    </li>
                <% } %>
            </ul>
        </div>
        <!--/.nav-collapse -->
    </div>
</nav>